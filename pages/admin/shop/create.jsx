import { SubscriptionFormik } from "@/schemas/subscription";

const CreateSubscription = () => {
  return <SubscriptionFormik title={`Ajout d'un abonnement`} />;
};

export default CreateSubscription;
